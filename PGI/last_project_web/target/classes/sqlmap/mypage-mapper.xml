<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mypage">
<select id="point" resultType="Integer">
select point
from userinfo
where email= #{email}
</select>
<select id="bookmarked_post" resultType="mypage.BookmarkedPostVO">
select i.model_name, i.filepath,i.model_code, b.brand_name
from my_manual m left outer join MANUAL n on m.manual_code = n.manual_code
                left outer join MODEL_INFO i on n.model_code = i.MODEL_CODE
                left outer join BRAND b on i.brand_code = b.brand_code
where email=#{email}
</select>
<select id="profile" resultType="String">
select filepath
from userinfo
where email=#{email}
</select>
<select id="download" resultType="mypage.DownloadVO">
select * 
from download_manual dm left outer join model_info mi on dm.model_code=mi.model_code
where email=#{email}
</select>
<select id="mypost" resultType="mypage.BoardVO">
select board_id,title,content,writedate
from board
where email=#{email}
</select>
<select id="myreply" resultType="mypage.ReplyVO">
select rep_no,content,writedate,email
from reply
where email=#{email}
</select>
</mapper>